package de.dpa.oss.metadata.mapper.imaging.backend.exiftool;

import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.core.IsNull.notNullValue;

public class StructEntryWriterTest
{
    /*
    @Test
    public void shouldCreateSimpleStruct()
    {
        // given
        StructEntryWriter structEntryWriter = StructEntryWriter.beginStruct();

        // when
        structEntryWriter.write( "field1", "value1");
        structEntryWriter.write( "field2", "value2");

        String structString = structEntryWriter.endStruct();

        // then
        assertThat( structString, is(notNullValue()));
        assertThat( structString, is( "{field1=value1,field2=value2}"));
    }

    @Test
    public void shouldWriteSingleEntry()
    {
        // given
        StructEntryWriter structEntryWriter = StructEntryWriter.beginStruct();

        // when
        structEntryWriter.write( "field1", "value1");

        String structString = structEntryWriter.endStruct();

        // then
        assertThat( structString, is(notNullValue()));
        assertThat( structString, is( "{field1=value1}"));

    }   */
}