package de.dpa.oss.common;

/**
 * Used to map a String from one character representation to another
 * @author oliver langer
 */
public interface StringCharacterMapping
{
    String map(String inputString);
}
